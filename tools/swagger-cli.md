# 💻 Swagger-CLI Commands 🐉

---

## ✅ Verification 

```bash
swagger-cli --version     									# 🛠️ Show the installed Swagger-CLI version

which swagger-cli       									# 📍 Show the path to the installed Swagger-CLI executable

swagger-cli --help      									# ❓ Display help information for Swagger-CLI
```